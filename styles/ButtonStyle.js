import {StyleSheet} from 'react-native'
const ButtonStyle = StyleSheet.create({
    buttonContainer: {
        flexWrap: 'wrap', 
        alignItems: 'flex-start',
        flexDirection:'row',
    },
    button: {
        flexDirection:'row',
        height: 40,
        width: 40

    }
});
export {ButtonStyle}