import {StyleSheet} from 'react-native'
const ButtonStyle = StyleSheet.create({
    buttonContainer: {
        flexWrap: 'wrap', 
        alignItems: 'flex-start',
        flexDirection:'row',
    },
    button: {
        flexDirection:'row'
    }
});
export {ButtonStyle}